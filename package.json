{
  "name": "traq-markdown-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/uni-kakurenbo/traq-markdown-engine.git"
  },
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "bootstrap": "bun install",
    "build": "bun run build:core && bun run build:commonmark && bun run build:trap-extension && bun run build:traq",
    "build:commonmark": "bun run --cwd packages/commonmark build",
    "build:core": "bun run --cwd packages/core build",
    "build:trap-extension": "bun run --cwd packages/trap-extension build",
    "build:traq": "bun run --cwd packages/traq build",
    "check": "bun run format:check && bun run lint:check && bun run build && bun run typecheck && bun run test && bun run lint:rust && bun run verify",
    "check:architecture": "bun run scripts/checks/architecture.ts",
    "check:generated": "bun run generate:bindings && bun run scripts/check-generated.ts",
    "check:package": "bun run scripts/checks/package-contents.ts",
    "corpus:collect": "bun run scripts/corpus/fetch.ts",
    "corpus:compare": "bun run scripts/corpus/compare.ts",
    "corpus:report": "bun run scripts/corpus/report.ts",
    "ci": "bun ci && bun run check",
    "format": "bun run format:prettier && bun run format:rust && bun run format:go",
    "format:check": "bun run format:prettier:check && bun run format:rust:check && bun run format:go:check",
    "format:go": "bun run scripts/gofmt.ts",
    "format:go:check": "bun run scripts/gofmt.ts --check",
    "format:prettier": "prettier --cache --write .",
    "format:prettier:check": "prettier --cache --check .",
    "format:rust": "cargo fmt --manifest-path packages/core/Cargo.toml --all && cargo fmt --manifest-path packages/commonmark/Cargo.toml --all && cargo fmt --manifest-path packages/trap-extension/Cargo.toml --all && cargo fmt --manifest-path packages/traq/Cargo.toml --all",
    "format:rust:check": "cargo fmt --manifest-path packages/core/Cargo.toml --all --check && cargo fmt --manifest-path packages/commonmark/Cargo.toml --all --check && cargo fmt --manifest-path packages/trap-extension/Cargo.toml --all --check && cargo fmt --manifest-path packages/traq/Cargo.toml --all --check",
    "generate:bindings": "bun run scripts/generate-bindings.ts",
    "lint": "eslint --fix --cache .",
    "lint:check": "eslint --cache --max-warnings=0 .",
    "lint:no-fix": "eslint --cache .",
    "lint:rust": "cargo clippy --manifest-path packages/core/Cargo.toml --locked --workspace --all-targets --all-features -- -D warnings && cargo clippy --manifest-path packages/commonmark/Cargo.toml --locked --workspace --all-targets --all-features -- -D warnings && cargo clippy --manifest-path packages/trap-extension/Cargo.toml --locked --workspace --all-targets --all-features -- -D warnings && cargo clippy --manifest-path packages/traq/Cargo.toml --locked --workspace --all-targets --all-features -- -D warnings",
    "test": "bun run test:typescript && bun run test:corpus && bun run test:rust && bun run test:go",
    "test:corpus": "bun test scripts/corpus",
    "test:go": "go -C packages/core/go test -count=1 ./... && go -C packages/commonmark/go test -count=1 ./... && go -C packages/trap-extension/go test -count=1 ./... && go -C packages/traq/go test -count=1 ./... && go -C packages/traq/examples/go test -count=1 ./...",
    "test:rust": "cargo test --manifest-path packages/core/Cargo.toml --locked --workspace --all-features && cargo test --manifest-path packages/commonmark/Cargo.toml --locked --workspace --all-features && cargo test --manifest-path packages/trap-extension/Cargo.toml --locked --workspace --all-features && cargo test --manifest-path packages/traq/Cargo.toml --locked --workspace --all-features",
    "test:typescript": "bun run --cwd packages/core test && bun run --cwd packages/traq test",
    "typecheck": "node ./node_modules/@typescript/native/bin/tsc -p tsconfig.json && bun run typecheck:core && bun run typecheck:commonmark && bun run typecheck:trap-extension && bun run typecheck:traq",
    "typecheck:commonmark": "bun run --cwd packages/commonmark typecheck",
    "typecheck:core": "bun run --cwd packages/core typecheck",
    "typecheck:trap-extension": "bun run --cwd packages/trap-extension typecheck",
    "typecheck:traq": "bun run --cwd packages/traq typecheck",
    "verify": "bun run check:architecture && bun run check:package && bun run check:generated"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@stylistic/eslint-plugin": "5.10.0",
    "@trivago/prettier-plugin-sort-imports": "6.0.2",
    "@types/bun": "1.4.0",
    "@typescript-eslint/eslint-plugin": "8.69.0",
    "@typescript-eslint/parser": "8.69.0",
    "esbuild": "0.28.2",
    "eslint": "10.9.1",
    "eslint-config-prettier": "10.1.8",
    "eslint-plugin-unused-imports": "4.4.1",
    "globals": "17.12.0",
    "jiti": "2.7.0",
    "jest-diff": "30.5.1",
    "parse5": "8.0.1",
    "prettier": "3.9.6",
    "@typescript/native": "npm:typescript@7.0.2",
    "typescript": "6.0.3"
  },
  "packageManager": "bun@1.3.14"
}
