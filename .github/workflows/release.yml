name: Release npm and Go packages

on:
  workflow_dispatch:
    inputs:
      release_type:
        description: Version component to increment
        required: true
        default: patch
        type: choice
        options:
          - patch
          - minor
          - major
      dry_run:
        description: Validate without committing, tagging, or publishing
        required: true
        default: true
        type: boolean

permissions:
  contents: read

concurrency:
  group: package-publish
  queue: max
  cancel-in-progress: false

jobs:
  publish:
    runs-on: ubuntu-24.04

    permissions:
      contents: write
      id-token: write

    env:
      DEFAULT_BRANCH: ${{ github.event.repository.default_branch }}
      PUBLISH: ${{ inputs.dry_run == false }}

    steps:
      - name: Require default branch workflow
        if: github.ref_name != github.event.repository.default_branch
        run: |
          echo "Run this workflow from $DEFAULT_BRANCH."
          exit 1

      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          ref: ${{ github.event.repository.default_branch }}
          fetch-depth: 0
          persist-credentials: false

      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24.14.1
          package-manager-cache: false

      - name: Calculate release version
        run: |
          current_version="$(node -p "require('./package.json').version")"

          if [[ ! "$current_version" =~ ^([0-9]+)\.([0-9]+)\.([0-9]+)$ ]]; then
            echo "Expected a stable SemVer version, got $current_version."
            exit 1
          fi

          major="${BASH_REMATCH[1]}"
          minor="${BASH_REMATCH[2]}"
          patch="${BASH_REMATCH[3]}"

          case "${{ inputs.release_type }}" in
            major) release_version="$((major + 1)).0.0" ;;
            minor) release_version="$major.$((minor + 1)).0" ;;
            patch) release_version="$major.$minor.$((patch + 1))" ;;
          esac

          echo "RELEASE_VERSION=$release_version" >> "$GITHUB_ENV"
          echo "RELEASE_TAG=v$release_version" >> "$GITHUB_ENV"

      - run: npm install --global npm@11.17.0
      - run: npm --version

      - uses: oven-sh/setup-bun@0c5077e51419868618aeaa5fe8019c62421857d6 # v2.2.0
        with:
          bun-version: 1.3.14

      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: 1.26.0

      - uses: dtolnay/rust-toolchain@6c977a6ca4077a0ceb28ffbe03f59d46e9ac8772 # v1
        with:
          toolchain: 1.96.0
          targets: wasm32-unknown-unknown
          components: clippy,rustfmt

      - run: bun ci

      - name: Synchronize package metadata with the release tag
        run: bun run release -- "$RELEASE_TAG" --prepare

      - name: Synchronize Go module dependencies with the release tag
        run: bun run scripts/release-go.ts "$RELEASE_TAG" --prepare

      - name: Validate shared version and internal dependencies
        run: |
          bun run release -- "$RELEASE_TAG" --check
          bun run scripts/release-go.ts "$RELEASE_TAG" --check

      - run: bun run check

      - name: Commit release metadata
        if: env.PUBLISH == 'true'
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          repository_url="https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          release_sha="$(git rev-parse HEAD)"
          git fetch --no-tags "$repository_url" "refs/heads/$DEFAULT_BRANCH"
          if [ "$(git rev-parse FETCH_HEAD)" != "$release_sha" ]; then
            echo "$DEFAULT_BRANCH changed while the release was being built."
            exit 1
          fi
          mapfile -t go_tags < <(bun run scripts/release-go.ts "$RELEASE_TAG" --tags)
          for tag in "$RELEASE_TAG" "${go_tags[@]}"; do
            if git ls-remote --exit-code --tags "$repository_url" "refs/tags/$tag" >/dev/null; then
              echo "Release tag $tag already exists."
              exit 1
            fi
          done
          git add \
            package.json bun.lock go.work \
            packages/core/package.json packages/plugins/commonmark/package.json \
            packages/plugins/traq/package.json packages/sdk/package.json \
            packages/core/go/go.mod packages/core/go/go.sum \
            packages/plugins/commonmark/go/go.mod packages/plugins/commonmark/go/go.sum \
            packages/plugins/traq/go/go.mod packages/plugins/traq/go/go.sum \
            packages/sdk/go/go.mod packages/sdk/go/go.sum packages/sdk/go/parser.wasm \
            packages/sdk/examples/go/go.mod packages/sdk/examples/go/go.sum
          if git diff --cached --quiet; then exit 0; fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git commit -m "chore(release): $RELEASE_VERSION"
          git push "$repository_url" "HEAD:refs/heads/$DEFAULT_BRANCH"

      - name: Release all four packages in dependency order
        run: |
          if [ "$PUBLISH" = true ]; then
            bun run release -- "$RELEASE_TAG" --publish
          else
            bun run release -- "$RELEASE_TAG" --dry-run
          fi

      - name: Tag npm release and all Go modules at the same commit
        if: env.PUBLISH == 'true'
        env:
          GITHUB_TOKEN: ${{ github.token }}
        run: |
          repository_url="https://x-access-token:${GITHUB_TOKEN}@github.com/${GITHUB_REPOSITORY}.git"
          mapfile -t go_tags < <(bun run scripts/release-go.ts "$RELEASE_TAG" --tags)
          tag_refs=("refs/tags/$RELEASE_TAG")
          git tag -a "$RELEASE_TAG" -m "$RELEASE_TAG"
          for tag in "${go_tags[@]}"; do
            git tag -a "$tag" -m "$tag"
            tag_refs+=("refs/tags/$tag")
          done
          git push --atomic "$repository_url" "${tag_refs[@]}"

      - name: Verify published Go modules outside the workspace
        if: env.PUBLISH == 'true'
        run: |
          for attempt in 1 2 3; do
            if bun run scripts/checks/go-consumer.ts "$RELEASE_TAG" --remote; then
              exit 0
            fi
            if [ "$attempt" -lt 3 ]; then sleep 10; fi
          done
          exit 1

      - name: Create GitHub Release
        if: env.PUBLISH == 'true'
        uses: softprops/action-gh-release@3d0d9888cb7fd7b750713d6e236d1fcb99157228 # v3.0.2
        env:
          GITHUB_TOKEN: ${{ github.token }}
        with:
          tag_name: ${{ env.RELEASE_TAG }}
          generate_release_notes: true
